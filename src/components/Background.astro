---
import { StarsBackground } from '@/premade/stars';
---

<StarsBackground
  client:load
  className="fixed inset-0 w-full h-full -z-10"
  factor={0.05}
  speed={20}
  pointerEvents={false}
  count1={1000}
  count2={240}
  count3={100}
  starOpacity={0.2}
/>
